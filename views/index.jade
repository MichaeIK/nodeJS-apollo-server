extends layout

block content
  h1= title
  p Welcome to #{title} Michael!
  p freemem: #{systemStatus.freemem} 
  p loadLast: #{systemStatus.loadLast} 
  p loadFiveMinutes: #{systemStatus.loadFiveMinutes} 
  p loadFifteenMinutes: #{systemStatus.loadFifteenMinutes} 
  p cpus: #{systemStatus.cpus} 